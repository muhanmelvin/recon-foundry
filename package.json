{
  "name": "recon-foundry",
  "version": "0.1.0",
  "private": true,
  "description": "Forge complete synthetic lease-audit packages that tie to the cent — except exactly where you told them to cheat. Runs entirely in the browser.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "vite build && node scripts/build-gate.mjs && node scripts/smoke-build.mjs",
    "build:single": "cross-env SINGLE_FILE=1 vite build && node scripts/finalize-single.mjs && node scripts/build-gate.mjs dist-single && node scripts/smoke-build.mjs dist-single",
    "preview": "vite preview",
    "fixtures": "node tools/export-fixtures.mjs",
    "ci": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "ajv": "^8.20.0",
    "cross-env": "^10.1.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vite-plugin-singlefile": "^2.0.3",
    "vitest": "^4.1.11"
  }
}
